<UserControl x:Class="DevBar.Modules.Claude.ClaudeCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <DataTemplate x:Key="SessionTemplate">
            <Border Width="170" Height="150" Margin="0,0,8,0" Padding="12"
                    Background="{StaticResource BrushBgElevated}" CornerRadius="8"
                    BorderThickness="1" BorderBrush="{StaticResource BrushHairline}"
                    Cursor="Hand" MouseLeftButtonUp="Session_Click" Tag="{Binding}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Ellipse Width="7" Height="7" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource BrushGood}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding State}" Value="WaitingOnYou">
                                            <Setter Property="Fill" Value="{StaticResource BrushWarn}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding State}" Value="Idle">
                                            <Setter Property="Fill" Value="{StaticResource BrushMuted}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding State}" Style="{StaticResource Overline}" Margin="6,0,0,0"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Text="{Binding Label}" Style="{StaticResource Body}"
                               TextWrapping="Wrap" TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>
                    <TextBlock Grid.Row="2" Text="click to focus" Style="{StaticResource BodyMuted}" FontSize="9.5"/>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <TextBlock x:Name="EmptyHint" Style="{StaticResource BodyMuted}"
                   HorizontalAlignment="Left" VerticalAlignment="Top"
                   Text="No Claude Code sessions detected."/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ThinScrollViewer}">
            <ItemsControl x:Name="List" ItemTemplate="{StaticResource SessionTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
